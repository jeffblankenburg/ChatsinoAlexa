{
  "name": "chatsino-alexa",
  "version": "1.1.0",
  "description": "a starter template for building alexa skills",
  "main": "index.js",
  "scripts": {
    "dev": "cd lambda && bst proxy lambda index.js",
    "test": "cd lambda && cd chatsino && jest --detectOpenHandles"
  },
  "author": "Jeff Blankenburg",
  "license": "ISC",
  "dependencies": {
    "ask-sdk-local-debug": "^1.1.0",
    "num-words": "^1.2.0"
  },
  "devDependencies": {
    "jest": "^26.4.2"
  }
}
